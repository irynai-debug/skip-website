# Production asset registry

Canonical supplied media remains immutable in `input/` and is imported explicitly by `src/siteContent.js`. Project-owned generated media lives under `public/assets/skip/`. The normalized `references/` copies exist only for queue validation.
